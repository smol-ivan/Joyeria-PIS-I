# Import controlador

class UI_Compra:
    '''Clase que despliega la interfaz de compras
    '''
    def __init__(self) -> None:
        # controlador como atributo
        pass

    def menu_compra(self, productos: list[dict] = None) -> None:
        '''Metodo que despliega el menu de compras
        '''
        pass

    """ 
    Pedir al controlador si existen datos de envio de usuario y forma de pago

    if controlador.not puede_comprar():
        print("No tienes datos de envio o de pago. por favor agrega datos antes de realizar la compra")
    # Si hay datos envio y pago
    Muestras la ui de compra
    controlador.realixar_compra()

    """